<?xml version="1.0" encoding="UTF-8"?>

<project name="ci-cd-project" default="test" basedir=".">

    <property name="src.dir" value="src"/>
    <property name="test.dir" value="test"/>
    <property name="web.dir" value="webapp"/>

    <property name="build.dir" value="build"/>
    <property name="classes.dir" value="${build.dir}/classes"/>
    <property name="test.classes.dir" value="${build.dir}/test-classes"/>
    <property name="reports.dir" value="${build.dir}/test-reports"/>
    <property name="dist.dir" value="${build.dir}/dist"/>

    <property name="war.name" value="ci-cd-project.war"/>

    <path id="compile.classpath">
        <pathelement location="/usr/share/java/servlet-api.jar"/>
    </path>

    <path id="test.classpath">
        <pathelement location="${classes.dir}"/>
        <pathelement location="${test.classes.dir}"/>
        <pathelement location="/usr/share/java/junit4.jar"/>
    </path>

    <target name="clean">
        <delete dir="${build.dir}"/>
    </target>

    <target name="compile">
        <mkdir dir="${classes.dir}"/>

        <javac srcdir="${src.dir}"
               destdir="${classes.dir}"
               includeantruntime="false">
            <classpath refid="compile.classpath"/>
        </javac>

        <echo message="Java compilation completed successfully!"/>
    </target>

    <target name="compile-test" depends="compile">
        <mkdir dir="${test.classes.dir}"/>

        <javac srcdir="${test.dir}"
               destdir="${test.classes.dir}"
               includeantruntime="false">
            <classpath refid="test.classpath"/>
        </javac>

        <echo message="Test compilation completed successfully!"/>
    </target>

    <target name="test" depends="compile-test">
        <mkdir dir="${reports.dir}"/>

        <junit printsummary="yes" haltonfailure="yes">
            <classpath refid="test.classpath"/>

            <batchtest fork="yes">
                <fileset dir="${test.dir}">
                    <include name="**/*Test.java"/>
                </fileset>
            </batchtest>

            <formatter type="plain"/>
            <formatter type="xml"/>
        </junit>

        <echo message="JUnit tests completed successfully!"/>
    </target>

    <target name="war" depends="test">
        <mkdir dir="${dist.dir}"/>

        <war destfile="${dist.dir}/${war.name}"
             webxml="${web.dir}/WEB-INF/web.xml">

            <fileset dir="${web.dir}">
                <exclude name="WEB-INF/web.xml"/>
            </fileset>

            <classes dir="${classes.dir}"/>

        </war>

        <echo message="WAR file created successfully!"/>
    </target>

    <target name="build" depends="war">
        <echo message="Complete build successful!"/>
    </target>

</project>
